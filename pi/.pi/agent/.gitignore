# Pi agent transient files — not tracked in repo
auth.json
sessions/
*.bak*

# Machine-specific configs
models.json
llama-switch.json
settings.json

# Symlink to private vault — context files stay in brain/vault/, never tracked
context
