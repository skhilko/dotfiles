# Pi agent transient files — not tracked in repo
auth.json
sessions/
*.bak*

# Machine-specific configs
models.json
llama-switch.json
settings.json

# Symlink to private vault — direction files stay in brain/vault/, never tracked
direction
