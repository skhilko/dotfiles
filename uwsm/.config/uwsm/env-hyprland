# Run Hyprland/display on the built-in GPU.
# Aquamarine splits AQ_DRM_DEVICES on colons and does not accept escaped colons,
# so the PCI by-path symlink cannot be used here.
export AQ_DRM_DEVICES="/dev/dri/card2"

# debug
#export HYPRLAND_TRACE=1
#export AQ_TRACE=1
